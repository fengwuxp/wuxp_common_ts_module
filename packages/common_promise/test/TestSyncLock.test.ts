




describe("test sync lock", () => {

    test("sync",async () => {

        // const syncLock=new ConditionalLock();

        // 等待返回一个条件处理对象或者是数据
        // const handle=await syncLock.conditionalLock();

        // const result=handle("data");


    }, 10 * 1000);
});